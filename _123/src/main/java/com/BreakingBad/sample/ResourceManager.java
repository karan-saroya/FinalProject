package com.BreakingBad.sample;

import java.util.ArrayList;
/*
public class ResourceManager {
    Assignment[] allocateTasks(Task[] tasks, Resource[] resources, Manager manager)
    {
        int coef = tasks.length/resources.length;
        ArrayList<Assignment> assgs = new ArrayList<>();
        int i = 0;
      //  while(i < tasks.length && i < resources.length)
        //    assgs.add(new Assignment(tasks[i], resources[i], manager));
        return assgs.toArray(new Assignment[assgs.size()]);
    }
    String[] genResCal(Assignment[] assgs, Resource res)
    {
        ArrayList<String> sched = new ArrayList<>();
        for(Assignment assg : assgs)
        {
            if(assg.getResource().getEmpID() == res.getEmpID())
                sched.add(assg.getTask().toString());
        }
        return sched.toArray(new String[sched.size()]);
    }
}
*/